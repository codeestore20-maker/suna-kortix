{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install -g pnpm && cd apps/frontend && pnpm install && pnpm build",
    "runCommand": "cd apps/frontend && pnpm start --port $PORT"
  },
  "runtime": {
    "nodeVersion": "22"
  }
}
